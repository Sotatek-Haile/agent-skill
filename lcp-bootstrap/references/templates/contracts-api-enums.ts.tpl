// ============================================================
// Shared enums — used across features
// Add domain-specific enum values extracted from requirements.
// Convention: only include enums used by ≥2 features here.
// ============================================================

// --- Sort direction (universal) ---
export enum SortDirection {
  ASC = 'ASC',
  DESC = 'DESC',
}

// TODO: Add domain-specific enums below based on requirements.
// Replace these examples with the project's actual domain values.

// Example — Status enum:
// export enum Status {
//   Active = 'active',
//   Inactive = 'inactive',
//   Pending = 'pending',
// }

// Example — Resource type enum:
// export enum ResourceType {
//   TypeA = 'type_a',
//   TypeB = 'type_b',
// }
